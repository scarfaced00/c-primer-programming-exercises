/* chapter 3-2 */
#include <stdio.h>

int main(void)
{
    int ascii;
    printf("Enter an ASCII code value: ");
    scanf("%d",&ascii);
    printf("The character for ASCII code you just entered: %c",ascii);
    
    return 0;
}
